{
  "name": "yasulfactory-rss",
  "version": "1.0.0",
  "description": "서예린의 은밀한 실험실 - RSS & 채널 대시보드",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "esbuild public/analytics.js --bundle --format=esm --outfile=public/dist/analytics.js",
    "build:watch": "npm run build -- --watch"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
